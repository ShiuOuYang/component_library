# 開發環境
#
# 只列出程式碼真的會讀取的變數（另見 .env.example）。
# 要連到實際的後端時，請在 .env.local 覆寫 VITE_API_BASE_URL —— 那個檔案不會進版控。

# 後端 router 位址
VITE_API_BASE_URL=http://localhost:3005/router

# 是否跳過登入檢查。開發模式本來就會跳過，這裡明寫出來只為讓它可被發現。
VITE_SKIP_AUTH=false
