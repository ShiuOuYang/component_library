# 生產環境
#
# ⚠️ VITE_API_BASE_URL 的實際值由 CI 注入（見 .github/workflows/deploy.yml），
#    不寫在版控裡。這裡留 localhost 只是佔位 —— 若建置時沒有注入，
#    CI 會直接失敗，而不是悄悄打包出一份連到 localhost 的前端。

VITE_API_BASE_URL=http://localhost:3007/router

VITE_SKIP_AUTH=false
