/**
 * ⚠️ 這個檔案「不屬於」元件庫，請放到使用元件庫的那個應用專案裡。
 *
 * 原本寫在元件庫的全域 style.css，等於規定所有下游 App 都必須滿版且不可捲動。
 * 元件庫不該決定宿主應用的版面。
 */

#app {
  width: 100%;
  height: 100vh;
  margin: 0;
  padding: 0;
  text-align: left;
}
