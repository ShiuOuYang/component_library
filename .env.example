# 環境變數範本
#
# 複製成 .env.local（已被 .gitignore 忽略）並填入你的值；
# 或由 CI / 部署環境注入。
#
# 只列出程式碼真的會讀取的變數 —— 加新變數時請一併更新這個檔案。

# ===== API =====
# 後端 router 的位址。未設定時會退回 src/api/index.js 裡的預設值。
VITE_API_BASE_URL=http://localhost:3007/router

# ===== 開發用開關 =====
# 設為 true 時跳過登入檢查。
# 注意：開發模式（import.meta.env.DEV）本來就會跳過，
# 這個變數是給「非開發模式但仍想跳過」的情境用的。
VITE_SKIP_AUTH=false
