<script lang="ts">
/**
 * HeaderLogoutButton —— ChptHeaderLogoutButton 的相容別名（@deprecated）
 *
 * 請改用 <ChptHeaderLogoutButton />。此檔僅為過渡期不破壞既有呼叫端而保留。
 *
 * ⚠️ 不可寫成 `export { default } from './ChptHeaderLogoutButton.vue'`：
 *    SFC 本身隱含一個 default export，兩者會撞成 TS2528。
 */
import ChptHeaderLogoutButton from './ChptHeaderLogoutButton.vue'

export default ChptHeaderLogoutButton
</script>
